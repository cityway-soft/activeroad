development:
  adapter: postgresql
  database: 'activeroads'
  template: template_postgis

test:
  adapter: postgresql
  database: 'activeroads_test'
  template: template_postgis

  # username: roads
  # password: roads0
  # host: osm.dbx1.dryade.priv

osm:
  adapter: postgresql
  database: osm
  #username: osm
  #password: osm
  # host: osm.dryade.priv
