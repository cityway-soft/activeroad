development:
  adapter: postgresql
  database: 'roads'
  template: template_postgis

test:
  adapter: postgresql
  database: 'roads_test'
  template: template_postgis

  # username: roads
  # password: roads0
  # host: osm.dbx1.dryade.priv

osm:
  adapter: postgresql
  database: osm
  #username: osm
  #password: osm
  # host: osm.dryade.priv
